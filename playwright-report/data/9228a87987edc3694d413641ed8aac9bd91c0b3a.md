# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.js >> TC-AUTH: Módulo de Autenticación >> TC-AUTH-001: Iniciar sesión con credenciales válidas
- Location: tests\auth.spec.js:46:3

# Error details

```
TimeoutError: page.waitForSelector: Timeout 20000ms exceeded.
Call log:
  - waiting for locator('#app') to be visible

```

# Page snapshot

```yaml
- generic [ref=e2]: Cannot GET /index.html
```

# Test source

```ts
  1   | // @ts-check
  2   | const { test, expect } = require('@playwright/test');
  3   | 
  4   | /**
  5   |  * Test suite: Autenticación
  6   |  * Covers: TC-AUTH-001 to TC-AUTH-006
  7   |  *
  8   |  * Prerequisites:
  9   |  * - Live Server running on http://localhost:5500
  10  |  * - Firebase project "pplataformas" accessible
  11  |  * - Test account exists: pasajero_test@javerianacali.edu.co / Test1234!
  12  |  */
  13  | 
  14  | const BASE_URL = '/index.html';
  15  | const VALID_EMAIL = 'pasajero_test@javerianacali.edu.co';
  16  | const VALID_PASSWORD = 'Test1234!';
  17  | 
  18  | /** Wait for the splash screen to disappear and the login screen to appear. */
  19  | async function waitForLoginScreen(page) {
> 20  |   await page.waitForSelector('#app', { state: 'visible' });
      |              ^ TimeoutError: page.waitForSelector: Timeout 20000ms exceeded.
  21  |   // Wait until the login form is rendered inside #app
  22  |   await page.waitForFunction(() => {
  23  |     const app = document.querySelector('#app');
  24  |     return app && app.innerHTML.includes('Iniciar sesión');
  25  |   }, { timeout: 15000 });
  26  | }
  27  | 
  28  | /** Fill login credentials and submit. */
  29  | async function doLogin(page, email, password) {
  30  |   const emailInput = page.locator('input[type="email"]').first();
  31  |   const passwordInput = page.locator('input[type="password"]').first();
  32  |   const submitBtn = page.locator('button').filter({ hasText: /iniciar sesión/i }).first();
  33  | 
  34  |   await emailInput.fill(email);
  35  |   await passwordInput.fill(password);
  36  |   await submitBtn.click();
  37  | }
  38  | 
  39  | test.describe('TC-AUTH: Módulo de Autenticación', () => {
  40  | 
  41  |   test.beforeEach(async ({ page }) => {
  42  |     await page.goto(BASE_URL);
  43  |     await waitForLoginScreen(page);
  44  |   });
  45  | 
  46  |   test('TC-AUTH-001: Iniciar sesión con credenciales válidas', async ({ page }) => {
  47  |     await doLogin(page, VALID_EMAIL, VALID_PASSWORD);
  48  | 
  49  |     // After successful login, the home screen should appear
  50  |     await expect(page.locator('#app')).toContainText(/bienvenido|hola|home/i, { timeout: 12000 });
  51  | 
  52  |     // Tab bar should be visible
  53  |   const tabBar = page.locator('.tab-bar').first();
  54  |   await expect(tabBar).toBeVisible({ timeout: 15000 });
  55  |   });
  56  | 
  57  |   test('TC-AUTH-002: Iniciar sesión con contraseña incorrecta', async ({ page }) => {
  58  |     await doLogin(page, VALID_EMAIL, 'ContraseñaIncorrecta99!');
  59  | 
  60  |     // An error message (toast or inline) should appear
  61  |     await page.waitForSelector('.toast', { timeout: 15000 });
  62  |   const errorIndicator = page.locator('.toast').first();
  63  |   await expect(errorIndicator).toContainText(/contraseña|incorrect|error/i);
  64  | 
  65  |     // The login form should still be visible (user not redirected)
  66  |     await expect(page.locator('input[type="email"]').first()).toBeVisible();
  67  |   });
  68  | 
  69  |   test('TC-AUTH-003: Iniciar sesión con email no registrado', async ({ page }) => {
  70  |     await doLogin(page, 'noexiste_xyz_abc@test.com', VALID_PASSWORD);
  71  | 
  72  |     await page.waitForSelector('.toast', { timeout: 15000 });
  73  |   const errorIndicator = page.locator('.toast').first();
  74  |   await expect(errorIndicator).toContainText(/usuario|encontrado|registrado|error/i);
  75  | 
  76  |     // Still on login screen
  77  |     await expect(page.locator('input[type="email"]').first()).toBeVisible();
  78  |   });
  79  | 
  80  |   test('TC-AUTH-004: Iniciar sesión con formato de email inválido', async ({ page }) => {
  81  |     await doLogin(page, 'noesun-email-valido', VALID_PASSWORD);
  82  | 
  83  |     // System should show a validation error (either native HTML or custom)
  84  |     // Check for native browser validation or custom error message
  85  |     const emailInput = page.locator('input[type="email"]').first();
  86  |     const isInvalid = await emailInput.evaluate(el => !el.validity.valid);
  87  | 
  88  |     if (isInvalid) {
  89  |       // Native browser validation caught it
  90  |       expect(isInvalid).toBe(true);
  91  |     } else {
  92  |       // Custom validation or Firebase error
  93  |       const errorIndicator = page.locator('.toast, [class*="error"], [class*="toast"]').first();
  94  |       await expect(errorIndicator).toBeVisible({ timeout: 10000 });
  95  |     }
  96  | 
  97  |     // Either way, user should not be logged in
  98  |     await expect(page.locator('input[type="email"]').first()).toBeVisible({ timeout: 5000 });
  99  |   });
  100 | 
  101 |   test('TC-AUTH-005: Registrar nuevo usuario con datos válidos', async ({ page }) => {
  102 |     // Navigate to the register form
  103 |     const registerLink = page.locator('button, a').filter({ hasText: /crear cuenta|registr/i }).first();
  104 |     await registerLink.click();
  105 | 
  106 |     // Fill registration form
  107 |     const nameInput = page.locator('input[type="text"], input[placeholder*="nombre" i]').first();
  108 |     const emailInput = page.locator('input[type="email"]').first();
  109 |     const passwordInput = page.locator('input[type="password"]').first();
  110 | 
  111 |     const uniqueEmail = `test_auto_${Date.now()}@javerianacali.edu.co`;
  112 | 
  113 |     await nameInput.fill('Usuario Playwright Test');
  114 |     await emailInput.fill(uniqueEmail);
  115 |     await passwordInput.fill('Test1234!');
  116 | 
  117 |     // Select passenger role if role selector exists
  118 |     const passengerOption = page.locator('button, label, input[value="passenger"]').filter({ hasText: /pasajero/i }).first();
  119 |     if (await passengerOption.isVisible()) {
  120 |       await passengerOption.click();
```