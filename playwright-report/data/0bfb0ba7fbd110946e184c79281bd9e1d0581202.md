# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: profile.spec.js >> TC-PROFILE: Perfil de Usuario >> TC-PROFILE-001: Cambiar rol de pasajero a conductor
- Location: tests\profile.spec.js:50:3

# Error details

```
TimeoutError: page.waitForSelector: Timeout 20000ms exceeded.
Call log:
  - waiting for locator('#app') to be visible

```

# Page snapshot

```yaml
- generic [ref=e2]: Cannot GET /index.html
```

# Test source

```ts
  1   | // @ts-check
  2   | const { test, expect } = require('@playwright/test');
  3   | 
  4   | /**
  5   |  * Test suite: Perfil de Usuario
  6   |  * Covers: TC-PROFILE-001, TC-PROFILE-002, TC-PROFILE-003
  7   |  *
  8   |  * Prerequisites:
  9   |  * - Live Server running on http://localhost:5501
  10  |  * - Test account: pasajero_test@javerianacali.edu.co / Test1234! (with role "passenger")
  11  |  */
  12  | 
  13  | const BASE_URL = '/index.html';
  14  | const PASSENGER_EMAIL = 'pasajero_test@javerianacali.edu.co';
  15  | const TEST_PASSWORD = 'Test1234!';
  16  | 
  17  | async function waitForLoginScreen(page) {
> 18  |   await page.waitForSelector('#app', { state: 'visible' });
      |              ^ TimeoutError: page.waitForSelector: Timeout 20000ms exceeded.
  19  |   await page.waitForFunction(() => {
  20  |     const app = document.querySelector('#app');
  21  |     return app && app.innerHTML.includes('Iniciar sesión');
  22  |   }, { timeout: 15000 });
  23  | }
  24  | 
  25  | async function loginAs(page, email, password) {
  26  |   await page.goto(BASE_URL);
  27  |   await waitForLoginScreen(page);
  28  | 
  29  |   await page.locator('input[type="email"]').first().fill(email);
  30  |   await page.locator('input[type="password"]').first().fill(password);
  31  |   await page.locator('button').filter({ hasText: /iniciar sesión/i }).first().click();
  32  | 
  33  |   await expect(page.locator('#app')).toContainText(/viajes|disponibles|hola|bienvenido/i, { timeout: 15000 });
  34  | }
  35  | 
  36  | async function navigateToProfile(page) {
  37  |   const profileTab = page.locator('[class*="tab"], button, a').filter({ hasText: /perfil|profile/i }).first();
  38  |   await expect(profileTab).toBeVisible({ timeout: 8000 });
  39  |   await profileTab.click();
  40  |   await page.waitForTimeout(1000);
  41  | }
  42  | 
  43  | test.describe('TC-PROFILE: Perfil de Usuario', () => {
  44  | 
  45  |   test.beforeEach(async ({ page }) => {
  46  |     await loginAs(page, PASSENGER_EMAIL, TEST_PASSWORD);
  47  |     await navigateToProfile(page);
  48  |   });
  49  | 
  50  |   test('TC-PROFILE-001: Cambiar rol de pasajero a conductor', async ({ page }) => {
  51  |     // Find the role selector — could be a toggle, select, or buttons
  52  |     const driverOption = page.locator('button, label, select option').filter({ hasText: /conductor|driver/i }).first();
  53  | 
  54  |     if (!(await driverOption.isVisible({ timeout: 8000 }))) {
  55  |       test.skip(true, 'Selector de rol no visible en pantalla de perfil.');
  56  |       return;
  57  |     }
  58  | 
  59  |     await driverOption.click();
  60  | 
  61  |     // Wait for the role to update
  62  |     await page.waitForTimeout(2000);
  63  | 
  64  |     // The "Publicar" tab should now be visible in the tab bar
  65  |     const publishTab = page.locator('[class*="tab"], button, a').filter({ hasText: /publicar|publish/i }).first();
  66  |     await expect(publishTab).toBeVisible({ timeout: 10000 });
  67  | 
  68  |     // The role badge in profile should update
  69  |     const roleBadge = page.locator('[class*="badge"], [class*="role"], [class*="chip"]').first();
  70  |     if (await roleBadge.isVisible()) {
  71  |       await expect(roleBadge).toContainText(/conductor|driver/i);
  72  |     }
  73  | 
  74  |     // Cleanup: switch back to passenger to not affect other tests
  75  |     const passengerOption = page.locator('button, label, select option').filter({ hasText: /pasajero|passenger/i }).first();
  76  |     if (await passengerOption.isVisible()) {
  77  |       await passengerOption.click();
  78  |       await page.waitForTimeout(1500);
  79  |     }
  80  |   });
  81  | 
  82  |   test('TC-PROFILE-002: Activar modo oscuro desde el perfil', async ({ page }) => {
  83  |     // Find the dark mode toggle
  84  |     const darkModeToggle = page.locator('input[type="checkbox"], [class*="toggle"], [class*="switch"]').filter({
  85  |       has: page.locator('[class*="dark"], [class*="oscuro"]').or(
  86  |         page.locator(':near(:text("oscuro")), :near(:text("dark"))')
  87  |       )
  88  |     }).first();
  89  | 
  90  |     // Alternative approach: find any toggle near "oscuro" text
  91  |     const toggleArea = page.locator('text=/oscuro|dark mode/i').locator('..').locator('input[type="checkbox"], [class*="toggle"]').first();
  92  | 
  93  |     let toggle = darkModeToggle;
  94  |     if (!(await toggle.isVisible({ timeout: 3000 }))) {
  95  |       toggle = toggleArea;
  96  |     }
  97  | 
  98  |     if (!(await toggle.isVisible({ timeout: 5000 }))) {
  99  |       test.skip(true, 'Toggle de modo oscuro no encontrado en perfil.');
  100 |       return;
  101 |     }
  102 | 
  103 |     // Record current state
  104 |     const wasChecked = await toggle.isChecked().catch(() => false);
  105 | 
  106 |     // Click the toggle to activate dark mode
  107 |     await toggle.click();
  108 |     await page.waitForTimeout(1000);
  109 | 
  110 |     // Check localStorage for the theme value
  111 |     const themeValue = await page.evaluate(() => localStorage.getItem('jv-theme'));
  112 |     expect(themeValue).toBe('dark');
  113 | 
  114 |     // The document should reflect dark mode (look for dark class on body or html)
  115 |     const bodyClass = await page.evaluate(() => document.body.className + ' ' + document.documentElement.className);
  116 |     expect(bodyClass).toMatch(/dark/i);
  117 | 
  118 |     // Reload and verify persistence
```